import { Download, FileText, MoreHorizontal, Upload } from "lucide-react";
import { useRef, useState } from "react";
import { resumes } from "../data/mockData";
import { Badge, Button, Card, Modal, SectionHeader } from "../components/ui";

export default function Resumes() {
  const [open,setOpen]=useState(false); const input=useRef();
  return <div className="animate-float-in space-y-6"><SectionHeader title="Resumes" description="Manage the versions you use across applications." action={<Button onClick={()=>setOpen(true)}><Upload size={14}/> Upload resume</Button>}/><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{resumes.map(r=><Card key={r.id} className="p-5"><div className="flex items-start justify-between"><div className="grid h-10 w-10 place-items-center rounded-lg bg-zinc-100 text-zinc-600 dark:bg-zinc-900 dark:text-zinc-300"><FileText size={18}/></div><button className="text-zinc-400"><MoreHorizontal size={17}/></button></div><h3 className="mt-5 text-sm font-semibold">{r.name}</h3><div className="mt-2 flex items-center gap-2"><span className="text-[10px] text-zinc-500">{r.size} · Updated {r.updated}</span>{r.default&&<Badge tone="green">Default</Badge>}</div><div className="mt-5 grid grid-cols-2 gap-2 text-[10px]"><div className="rounded-lg bg-zinc-50 p-3 dark:bg-zinc-900"><span className="text-zinc-400">Used in</span><p className="mt-1 font-semibold">{r.used} applications</p></div><Button variant="secondary"><Download size={13}/> Download</Button></div></Card>)}</div><Modal open={open} onClose={()=>setOpen(false)} title="Upload resume"><div onClick={()=>input.current?.click()} className="cursor-pointer rounded-xl border border-dashed border-zinc-300 p-10 text-center hover:bg-zinc-50 dark:border-zinc-700 dark:hover:bg-zinc-900"><Upload className="mx-auto text-zinc-400"/><p className="mt-3 text-xs font-medium">Drop your PDF here or browse</p><p className="mt-1 text-[10px] text-zinc-400">PDF up to 5MB</p><input ref={input} type="file" accept=".pdf" className="hidden"/></div><div className="mt-5 flex justify-end"><Button onClick={()=>setOpen(false)}>Done</Button></div></Modal></div>;
}